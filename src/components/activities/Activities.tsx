import React from 'react';
import './activities.scss';

type ActivitiesProps = {
};

const Activities: React.FC<ActivitiesProps> = (props: ActivitiesProps) => {
    return (
      <>
        Activities Page
      </>
    );
};

export default Activities;