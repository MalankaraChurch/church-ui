import React from 'react';
import './events.scss';

type EventsProps = {
};

const Events: React.FC<EventsProps> = (props: EventsProps) => {
    return (
      <>
        Events Page
      </>
    );
};

export default Events;