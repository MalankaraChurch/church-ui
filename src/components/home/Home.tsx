import React from 'react';
import './Home.scss';

type HomeProps = {
};

const Home: React.FC<HomeProps> = (props: HomeProps) => {
    return (
      <>
        Home Page
      </>
    );
};

export default Home;