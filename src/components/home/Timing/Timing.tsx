import React from 'react';
import './Timing.scss';

type TimingProps = {
};

const Timing: React.FC<TimingProps> = (props: TimingProps) => {
    return (
      <>
        Timing Section
      </>
    );
};

export default Timing;