import React from 'react';
import './Footer.scss';

type FooterProps = {
};

const Footer: React.FC<FooterProps> = (props: FooterProps) => {
    return (
      <>
        Footer Section
      </>
    );
};

export default Footer;