import React from 'react';
import './Shrine.scss';

type ShrineProps = {
};

const Shrine: React.FC<ShrineProps> = (props: ShrineProps) => {
    return (
      <>
        Shrine Section
      </>
    );
};

export default Shrine;